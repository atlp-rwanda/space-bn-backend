language: node_js

node_js:
  - 'stable'

services:
  - postgresql

before_script:
  - psql -c 'create database test_db;' -U postgres

script: 
  - npm install
  - sequelize db:migrate
  - sequelize db:seed:all
  - npm test

after_succes: 
  - npm run coverage
  