env:
  global:
    - NODE_ENV=test
language: node_js
node_js:
 - "12.18.4"
install:
  - npm install
services:
  - postgresql
before_script:
  - psql -c 'create database test_db;' -U postgres
script: 
  - npm test